= form_for @product do |f|
  = render 'layouts/errors', :obj => @article  if @product.errors.any? 
  = f.hidden_field :user_id, :value => current_user.id
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :desc
    = f.text_area :desc
  .actions
    = f.submit 'Save'
